spring:
  application:
    name: my-application
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    password: sa
    username: sa
  batch:
    initialize-schema: always
    job:
      enabled: false
